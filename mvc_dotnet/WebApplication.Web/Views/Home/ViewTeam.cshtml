@model List<Team>
@{
    ViewData["Title"] = "ViewTeam";
}

<h2>ViewTeam</h2>
@foreach (Team team in Model)
{
<form method="get" asp-controller="Home" asp-action="ViewTeam" asp-route-code="@team.League"></form>
}

<hr />
<table class="team_table">
    <tr>
        <th>Team Name</th>
        <th>League</th>
        <th>Organization</th>
        <th>Primary Venue</th>
        <th>Secondary Venue</th>
        <th>Home Dates Available</th>
        <th>Travel Dates Available</th>
    </tr>
    @foreach (Team team in Model)
    {

        <tr>
            <td>@team.Name</td>
            <td>@team.League</td>
            <td>@team.Org</td>
            <td>@team.PrimaryVenue</td>
            <td>@team.SecondaryVenue</td>
            <td>
                @foreach (DateTime date in team.HomeDates)
                {
                    @date.ToString("MM/dd/yyyy")

                }
            </td>
            <td>
                @foreach (DateTime date in team.TravelDates)
                {
                    @date.ToString("MM/dd/yyyy")

                }
            </td>
        </tr>
    }
</table>

<div>
    <a asp-action="Index">Back to Home Page</a>
</div>